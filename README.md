# NCLEX 7 Day Training — 重點整理

一份以繁體中文撰寫的 NCLEX 備考七日計畫重點筆記，以單頁 HTML 靜態網頁呈現。

## 使用方式

直接在瀏覽器開啟 `index.html`，或啟動本地伺服器：

```bash
python3 -m http.server 8081
```

接著前往 [http://localhost:8081](http://localhost:8081)。
